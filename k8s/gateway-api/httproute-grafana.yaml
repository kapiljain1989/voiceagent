apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: voiceagent
spec:
  parentRefs:
    - name: voiceagent-gateway
  hostnames:
    - "grafana.voiceagent.local"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: grafana
          port: 3000
