apiVersion: v1
kind: Service
metadata:
  name: piper
  namespace: voiceagent
spec:
  type: ClusterIP
  selector:
    app: piper
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
