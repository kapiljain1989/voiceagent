#!KAMAILIO
#
# VoiceAgent Local SBC Lab — Kamailio Configuration
#
# Routes softphone calls to FreeSWITCH (VoiceAgent gateway).
# Dial plan:
#   1xxx  → AI Agent (FreeSWITCH interactive mode)
#   2xxx  → Co-Pilot bridge (customer ↔ agent) + SIPREC
#   21xx  → Co-Pilot auto-answer (FreeSWITCH answers agent leg)
#   3xxx  → Human queue (FreeSWITCH hold audio)

####### Global Parameters #########

debug=2
log_stderror=yes
fork=yes
children=4

listen=udp:0.0.0.0:5060
listen=tcp:0.0.0.0:5060

#!substdef "!EXTIP!__EXT_IP__!g"
advertised_address=EXTIP
advertised_port=5060

#!substdef "!FSHOST!freeswitch!g"
#!substdef "!FSPORT!5060!g"

mpath="/usr/lib/kamailio/modules/"

####### Modules Section ########

loadmodule "kex.so"
loadmodule "tm.so"
loadmodule "tmx.so"
loadmodule "sl.so"
loadmodule "rr.so"
loadmodule "pv.so"
loadmodule "maxfwd.so"
loadmodule "textops.so"
loadmodule "siputils.so"
loadmodule "xlog.so"
loadmodule "sanity.so"
loadmodule "usrloc.so"
loadmodule "registrar.so"

# Module parameters
modparam("registrar", "default_expires", 3600)
modparam("registrar", "min_expires", 60)
modparam("registrar", "max_expires", 7200)

modparam("usrloc", "db_mode", 0)

modparam("rr", "enable_full_lr", 1)
modparam("rr", "append_fromtag", 1)

modparam("tm", "fr_timer", 5000)
modparam("tm", "fr_inv_timer", 30000)

####### Routing Logic ########

request_route {
    if (!mf_process_maxfwd_header("10")) {
        sl_send_reply("483", "Too Many Hops");
        exit;
    }

    if (!sanity_check("1511", "7")) {
        xlog("L_WARN", "Malformed SIP from $si:$sp\n");
        exit;
    }

    force_rport();

    if (is_method("INVITE|SUBSCRIBE")) {
        record_route();
    }

    # In-dialog requests
    if (has_totag()) {
        if (loose_route()) {
            route(RELAY);
            exit;
        }
        if (is_method("ACK")) {
            if (t_check_trans()) {
                route(RELAY);
                exit;
            }
            exit;
        }
        sl_send_reply("404", "Not Found");
        exit;
    }

    if (is_method("CANCEL")) {
        if (t_check_trans()) {
            t_relay();
        }
        exit;
    }

    t_check_trans();

    # REGISTER
    if (is_method("REGISTER")) {
        xlog("L_INFO", "REGISTER from $fU@$fd via $si:$sp\n");
        if (!save("location")) {
            sl_reply_error();
        }
        exit;
    }

    # OPTIONS
    if (is_method("OPTIONS")) {
        sl_send_reply("200", "OK");
        exit;
    }

    # INVITE
    if (is_method("INVITE")) {
        route(ROUTE_CALLS);
        exit;
    }

    if ($rU == $null) {
        sl_send_reply("484", "Address Incomplete");
        exit;
    }

    route(RELAY);
}

# ─── Call routing ────────────────────────────────────────────────

route[ROUTE_CALLS] {
    xlog("L_INFO", "INVITE from $fU to $rU\n");

    # 1xxx → Interactive AI Agent
    if ($rU =~ "^1[0-9]{3}$") {
        xlog("L_INFO", "→ AI Agent: $rU\n");
        $du = "sip:FSHOST:FSPORT;transport=udp";
        t_on_failure("FAIL_ROUTE");
        route(RELAY);
        exit;
    }

    # 21xx → Co-Pilot auto-answer
    if ($rU =~ "^21[0-9]{2}$") {
        xlog("L_INFO", "→ Co-Pilot auto-answer: $rU\n");
        $du = "sip:FSHOST:FSPORT;transport=udp";
        route(RELAY);
        exit;
    }

    # 2xxx → Co-Pilot (FreeSWITCH bridges to agent1 + SIPREC)
    if ($rU =~ "^2[0-9]{3}$") {
        xlog("L_INFO", "→ Co-Pilot: $rU\n");
        $du = "sip:FSHOST:FSPORT;transport=udp";
        route(RELAY);
        exit;
    }

    # 3xxx → Human queue
    if ($rU =~ "^3[0-9]{3}$") {
        xlog("L_INFO", "→ Human Queue: $rU\n");
        $du = "sip:FSHOST:FSPORT;transport=udp";
        route(RELAY);
        exit;
    }

    sl_send_reply("404", "No Route");
    exit;
}

# ─── Relay ───────────────────────────────────────────────────────

route[RELAY] {
    if (!t_relay()) {
        sl_reply_error();
    }
    exit;
}

# ─── Failure route ───────────────────────────────────────────────

failure_route[FAIL_ROUTE] {
    if (t_is_canceled()) {
        exit;
    }
    xlog("L_WARN", "Call failed: $rU status $T_reply_code\n");
    if (t_check_status("5[0-9][0-9]|6[0-9][0-9]")) {
        xlog("L_WARN", "Failover to human queue 3000\n");
        $rU = "3000";
        $du = "sip:FSHOST:FSPORT;transport=udp";
        t_relay();
        exit;
    }
}
