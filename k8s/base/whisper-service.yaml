apiVersion: v1
kind: Service
metadata:
  name: whisper
  namespace: voiceagent
spec:
  type: ClusterIP
  selector:
    app: whisper
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
