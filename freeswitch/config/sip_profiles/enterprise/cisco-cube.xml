<!-- Cisco CUBE (Unified Border Element) SIP trunk profile -->
<profile name="cisco-cube">
  <gateways>
    <gateway name="cube">
      <param name="proxy" value="$${sbc_address}"/>
      <param name="register" value="false"/>
      <param name="caller-id-in-from" value="true"/>
      <param name="ping" value="25"/>
      <param name="retry-seconds" value="10"/>
    </gateway>
  </gateways>
  <settings>
    <param name="sip-ip" value="$${local_ip_v4}"/>
    <param name="sip-port" value="5060"/>
    <param name="ext-sip-ip" value="$${ext_ip}"/>
    <param name="rtp-ip" value="$${local_ip_v4}"/>
    <param name="ext-rtp-ip" value="$${ext_ip}"/>
    <param name="context" value="public"/>
    <param name="dialplan" value="XML"/>
    <param name="auth-calls" value="false"/>
    <!-- Cisco CUBE codec negotiation -->
    <param name="inbound-codec-prefs" value="PCMU,PCMA,G722,G729"/>
    <param name="outbound-codec-prefs" value="PCMU,PCMA,G722"/>
    <param name="inbound-codec-negotiation" value="generous"/>
    <!-- CUBE-specific settings -->
    <param name="aggressive-nat-detection" value="false"/>
    <param name="enable-timer" value="true"/>
    <param name="session-timeout" value="1800"/>
    <param name="minimum-session-expires" value="120"/>
    <param name="rfc2833-pt" value="101"/>
    <param name="dtmf-type" value="rfc2833"/>
    <!-- TLS for enterprise -->
    <param name="tls" value="$${sbc_tls_enabled}"/>
    <param name="tls-sip-port" value="5061"/>
  </settings>
</profile>
