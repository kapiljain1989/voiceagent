apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: voiceagent
  labels:
    app: ui
spec:
  type: ClusterIP
  selector:
    app: ui
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
