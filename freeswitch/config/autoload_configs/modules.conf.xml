<configuration name="modules.conf" description="Module loading">
  <modules>
    <!-- Logging -->
    <load module="mod_console"/>
    <load module="mod_logfile"/>

    <!-- SIP stack -->
    <load module="mod_sofia"/>

    <!-- Dialplan + channel tools -->
    <load module="mod_dialplan_xml"/>
    <load module="mod_dptools"/>
    <load module="mod_commands"/>

    <!-- Codecs — L16 is critical for raw PCM pass-through -->
    <load module="mod_sndfile"/>
    <load module="mod_tone_stream"/>

    <!-- WebSocket audio forking — the bridge to our Go gateway -->
    <load module="mod_audio_fork"/>

    <!-- ESL for external control -->
    <load module="mod_event_socket"/>
  </modules>
</configuration>
